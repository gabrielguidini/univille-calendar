version: '3.8'

services:
  uc-db:
    image: postgres:latest
    container_name: uc-db
    restart: on-failure
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "5432:5432"
    networks: ['uc-network']

  uc-app:
    image: gabrielguidini/univille-calendar:latest
    container_name: uc-app
    restart: on-failure
    build:
      context: ./univille-calendar
      dockerfile: Dockerfile
    depends_on:
      - uc-db
    env_file:
      - .env
    environment:
      - SPRING_DATASOURCE_URL=${POSTGRES_URL}
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    ports:
      - "8080:8080"
    networks: ['uc-network']

networks: {uc-network: {}}